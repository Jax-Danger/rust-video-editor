[package]
name = "editor-media"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Media probe for Meridian (stub by default, ffprobe behind a feature)"
publish = false

[dependencies]
editor-core = { path = "../editor-core" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
# Shells out to `ffprobe` on PATH. The default build never links or invokes ffmpeg.
ffmpeg = []
