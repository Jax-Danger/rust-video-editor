[package]
name = "editor-media"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Media probe and preview decode for Meridian (stub by default, ffmpeg CLI behind a feature)"
publish = false

[dependencies]
editor-core = { path = "../editor-core" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
# Shell out to `ffprobe` and `ffmpeg` on PATH. The default build never invokes them.
ffmpeg = []
